# M1 acceptance evidence

Implemented scope: #7–#14 and #70 (tracker #78).

- KiCad 10 execution with timeouts, diagnostics, source identity, fresh export directories and hashes.
- Strict ERC/DRC interpretation preserving ignored/excluded rules, unconnected items and parity findings.
- Real Gerber, drill and STEP export control fixtures; source/report mutation detection.
- Audited stdio MCP operation mapping, deny-unknown/write permission and separate snapshot-only reviewer tools.
- Lossless persisted-state capture, content validation/diffs and mutation→readback→verification workflow.
- Fresh check-kicad CLI and Fedora 44 integration CI with archived execution artifacts.

Local Python 3.13 verification: 60 passed, 1 skipped (ngspice absent); Ruff passes.
KiCad 10.0.0 runs real ERC/DRC, successful exports and a deliberately failing outline-gap case.
A local stdio server fixture qualifies transport behavior, not a third-party live editing server.
Empty schematic/outline controls are test fixtures, not fabrication-ready electronics.

Full model orchestration, operating-system isolation, live editing-server qualification and
manufacturing-release consistency remain in later milestones. Issues remain open as requested.
