# M2 — Engineering Verification implementation evidence

Implemented issue scope: #15–#19, #21–#26, #28–#34, #71–#72. Issues remain open
as instructed; this document records implementation and limits rather than closing them.

- Canonical JSON/KiCad-style CSV BOM import, grouped references, MPN and metadata checks.
- Optional normalized supplier MCP adapter and real JLCSearch public HTTP adapter;
  lifecycle/stock/source diversity risk with explicit unknowns and mirror provenance.
- Family-data-driven FPGA bank voltage/pin assignment checks. No fabricated vendor data.
- Validated rail/load/sequencing graphs and conservative current propagation;
  regulator input/dropout/loss/thermal calculations with required source evidence.
- Declared decoupling, connector protection and accessible test-point coverage audits.
- Local hashed datasheet registry and evidence-backed numeric constraint checks.
- Bounded ngspice process execution, failed-log detection, declarative assertions,
  real ideal RC audio-filter and supply-transient workflows, integration tests.
- Golden PASS/FAIL/WARN/SKIP/ERROR release aggregation fixture.
- Reproducible external MCP selection and narrow SWIG qualification, plus inventory pinning.

Local validation: 80 tests passed on Python 3.13.14 (no skips); Ruff passed. Python 3.12
is also checked before publication. ngspice 47 measured the 20 kHz ideal RC cutoff and
expected supply droop/recovery within declared assertions; deliberately impossible
limits and missing simulator models fail. JLCSearch exact C21190/MPN lookup returned
WARN because manufacturer, stock source age and price currency were not supplied.
KiCad MCP open/read/route/save persisted one trace; independent KiCad 10.0.0 DRC reported
the intentional dangling end and ignored rules. These are integration controls only.

Read `docs/engineering-verification.md` and `docs/integrations.md` for limitations.
No PCB product has been designed, independently approved or released for fabrication.
