# Minimal specification

Numbers are proposed requirements, not component datasheet claims. Schema validation
passes; engineering completeness and all board/release gates remain unresolved. No PCB
is included. Expected `verify` exit code is 1 and `release` exit code is 1.
